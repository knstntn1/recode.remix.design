---
import BaseLayout from '../../layouts/BaseLayout.astro';

export function getStaticPaths() {
  return [{ params: { slug: 'platzhalter' } }];
}
---

<BaseLayout title="Exponat Platzhalter">
  <section class="intro-surface surface" aria-labelledby="exhibit-title">
    <h1 id="exhibit-title">Exponat Platzhalter</h1>
    <p>Statische Ausstellungsseite im Aufbau.</p>
    <p>Die Exponate und Medien werden in der nächsten Phase lokal migriert.</p>
  </section>
</BaseLayout>
