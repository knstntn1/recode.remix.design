---
phase: 4
plan: 1
type: execute
wave: 1
depends_on: []
files_modified:
  - scripts/verify-build-output.mjs
  - package.json
requirements:
  - QUAL-01
  - QUAL-02
  - QUAL-03
autonomous: true
must_haves:
  goal: "Production build output is automatically checked for route, link, and asset readiness."
  verification:
    - "npm run verify:build exits 0"
    - "npm run verify exits 0"
---

# Plan 04-01: Add Build Output Link and Asset Verification

## Tasks

- Add `scripts/verify-build-output.mjs`.
- Verify `dist/index.html` and all 17 exhibit pages exist.
- Verify internal links in built HTML resolve to generated pages or anchors.
- Verify local `/assets/recode-remix-design/...` references resolve to files in `dist/`.
- Verify built files contain no `experimente.joachim-wedekind.de/wp-content`.
- Add `verify:build` npm script and include it in `verify`.
